import React from 'react';
import Nav from '../nav';

const Header = (props) => (
    <header className="banner row" role="banner">
        <Nav/>        
    </header>
)

export default Header;