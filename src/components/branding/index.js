import React from 'react';
import BrandingTitle from './branding-title';

const Branding = (props) => (
    <div className="branding navbar-header">
        <BrandingTitle/>        
    </div>
)

export default Branding;